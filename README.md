# 夜航计划

一个无需构建工具的静态中文杂志首页，主题是城市夜晚、创作、独处和慢生活。

## 本地预览

直接双击 `index.html` 即可打开，也可以在当前目录运行：

```powershell
python -m http.server 8080
```

然后访问 <http://localhost:8080>。

页面内置：

- 响应式布局，适配手机和桌面端
- 夜间 / 日间色彩模式切换，并使用 `localStorage` 记忆选择
- 文章分类筛选
- 订阅表单的前端交互反馈
- 无外部图片依赖的抽象插画与视觉组件
